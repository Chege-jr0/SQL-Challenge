-- we use the in operators with the where clause to match the values in a list
-- we can also use the in operator to check if the values are in a mutiple choices.
 select first_name, country
 from customers_data
 where country in ('USA', 'UK');
 
 
 -- The IN operator can also be used to choose rows where a specific value is present in the specific field
select first_name, country
from customer_data
where 'USA' IN (country);

-- NOT IN OPERATOR
-- The not in operator excludes the rows that match values in the list
select first_name, country
from customer_data
where country not in ('UK', 'UAE');
